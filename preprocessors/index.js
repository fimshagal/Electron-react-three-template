const AssetsClassGenerator = require('./assets.class.generator/assets.class.generator');

const exeAssetsGeneration = () => {
    console.log(AssetsClassGenerator);
};

(() => {
    exeAssetsGeneration();
})();

// TODO finish "Assets Generation" preprocessor